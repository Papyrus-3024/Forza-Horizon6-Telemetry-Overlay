Current is good, have the following tasks: 

addl. meterics needs to be implemented still such as suspension  
refactor f9 page for better layout, ease of controlm, and create a setting icon component that brings up setting menu on hover, pin on click (no clickthrough)  
qol, saving captures as bin, parsed later. Might need to look into better storage system like parquet or pandas 
qol,  in f8 mode, allow drag corners to resize each components. 
qol features discussed before but not implemented or left in other phases. 