<Window x:Class="Fh6.Telemetry.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:layouts="clr-namespace:Fh6.Telemetry.Overlay.Layouts"
        xmlns:settings="clr-namespace:Fh6.Telemetry.Overlay.Settings"
        Title="FH6 Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Border x:Name="Root" Background="#01000000"
            MouseEnter="Root_MouseEnter" MouseLeave="Root_MouseLeave">
        <Grid>
            <!-- FreeLayout canvas fills the entire window -->
            <layouts:FreeLayout x:Name="FreeLayoutHost"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"/>

            <!-- Status text pinned top-left, non-movable -->
            <TextBlock Text="{Binding Status}"
                       Foreground="#FFE05A5A"
                       FontFamily="Consolas"
                       FontSize="11"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Margin="6,4,0,0"
                       IsHitTestVisible="False"/>

            <!-- Quit button: visible on hover, works when window is interactive (edit mode / not click-through).
                 F7 global hotkey is the gameplay-time quit. -->
            <Button x:Name="QuitButton"
                    Width="16" Height="16"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,2,2,0"
                    Opacity="0"
                    Click="QuitButton_Click"
                    ToolTip="Close overlay (F7)">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#A6080A08"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="3">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <TextBlock Text="✕" FontSize="9" Foreground="White"/>
            </Button>

            <!-- Settings gear: hover to peek the flyout, click to pin (F9 toggles pin). -->
            <Button x:Name="GearButton"
                    Width="26" Height="26"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,2,26,0" Opacity="0.55"
                    Click="GearButton_Click"
                    ToolTip="Settings — hover to peek, click to pin (F9)">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#A6080A08"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <TextBlock Text="⚙" FontSize="15" Foreground="White"/>
            </Button>

            <!-- In-overlay settings flyout (hidden until peeked/pinned). Positioned on a Canvas
                 so it can be dragged by its header and remember where the user put it. -->
            <Canvas x:Name="ChromeCanvas" IsHitTestVisible="True" Background="{x:Null}">
                <settings:SettingsFlyout x:Name="Settings" Visibility="Collapsed"/>
            </Canvas>
        </Grid>
    </Border>
</Window>
