<UserControl x:Class="Fh6.Telemetry.Overlay.Widgets.BoostWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#C8121821"
            BorderBrush="#26FFFFFF" BorderThickness="1"
            CornerRadius="8" Padding="10,6">
        <Border.Effect>
            <DropShadowEffect Color="#000000" ShadowDepth="0" BlurRadius="10" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel>
            <TextBlock Text="BOOST"
                       Foreground="#80FFFFFF" FontSize="8" FontFamily="Consolas"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding Boost}"
                           Foreground="#F2F4F7" FontSize="20" FontWeight="Bold" FontFamily="Consolas"/>
                <TextBlock Text=" psi"
                           Foreground="#80FFFFFF" FontSize="9" FontFamily="Consolas"
                           VerticalAlignment="Bottom" Margin="3,0,0,2"/>
            </StackPanel>
            <TextBlock Text="FUEL"
                       Foreground="#80FFFFFF" FontSize="8" FontFamily="Consolas"
                       Margin="0,4,0,0"/>
            <TextBlock Text="{Binding Fuel}"
                       Foreground="#99FFFFFF" FontSize="12" FontFamily="Consolas"/>
        </StackPanel>
    </Border>
</UserControl>
