<UserControl x:Class="Fh6.Telemetry.Overlay.Widgets.SpeedWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#C8121821"
            BorderBrush="#26FFFFFF" BorderThickness="1"
            CornerRadius="8" Padding="10,6">
        <StackPanel>
            <TextBlock Text="MPH"
                       Foreground="#80FFFFFF" FontSize="8" FontFamily="Consolas"/>
            <TextBlock Text="{Binding SpeedMph}"
                       Foreground="#F2F4F7" FontSize="34" FontWeight="Bold" FontFamily="Consolas"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding Speed}"
                           Foreground="#99FFFFFF" FontSize="12" FontFamily="Consolas"/>
                <TextBlock Text=" km/h"
                           Foreground="#80FFFFFF" FontSize="9" FontFamily="Consolas"
                           VerticalAlignment="Bottom" Margin="2,0,0,1"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
